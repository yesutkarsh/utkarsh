/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com','cdn.analyticsvidhya.com','www.itedgenews.africa']
      }
};

export default nextConfig;
